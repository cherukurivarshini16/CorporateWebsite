apiVersion: v1
kind: Service
metadata:
  name: corporatewebsite-service
spec:
  selector:
    app: corporatewebsite
  type: NodePort
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
      nodePort: 30080